require "spec_helper"

describe Shadowsocks do
  it "has a version number" do
    expect(Shadowsocks::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
